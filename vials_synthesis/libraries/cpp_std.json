{
  "id": "cpp_std_library",
  "concept": "C++ standard library documentation: data structures (std::map, etc.) and the <random> engine/distribution API",
  "confidence": 1.0,
  "evidence": ["C++ Standard Library reference — <map>, <random> (std::mt19937, std::uniform_int_distribution), <iostream>"],
  "facts": [
    ["cpp_std", "is_a", "data_library"],
    ["cpp_std", "provides", "dictionary_model"],
    ["cpp_std", "implements_language", "lang_cpp"],
    ["lang_cpp", "file_extension", "cpp"],
    ["lang_cpp", "build_command", "g++ main.cpp -o main"],
    ["lang_cpp", "run_command", "./main"],
    ["cpp_std", "code_banner", "// assembled by the DSCE polyglot assembler (C++)\n"],
    ["cpp_std", "code_random_cli", "#include <iostream>\n#include <random>\n\nint main() {\n    std::random_device rd;\n    std::mt19937 gen(rd());\n    std::uniform_int_distribution<> dist(1, 100);\n    std::cout << dist(gen) << std::endl;\n    return 0;\n}\n"],
    ["cpp_std", "code_gui_body", "#include <QApplication>\n#include <QLabel>\n#include <QVBoxLayout>\n#include <QWidget>\nint main(int argc, char *argv[]) {\n    QApplication app(argc, argv);\n    QWidget window;\n    window.setWindowTitle(\"C++ GUI\");\n    window.resize(300, 200);\n    QVBoxLayout *layout = new QVBoxLayout(&window);\n    QLabel *label = new QLabel(\"Hello from C++ Qt GUI!\", &window);\n    layout->addWidget(label);\n    window.show();\n    return app.exec();\n}\n"],
    ["cpp_std", "code_web_body", "#include <iostream>\n#include <string>\n#include <cstring>\n#include <sys/socket.h>\n#include <netinet/in.h>\n#include <unistd.h>\nint main() {\n    int server_fd = socket(AF_INET, SOCK_STREAM, 0);\n    sockaddr_in address{};\n    address.sin_family = AF_INET;\n    address.sin_addr.s_addr = INADDR_ANY;\n    address.sin_port = htons(8080);\n    bind(server_fd, (struct sockaddr*)&address, sizeof(address));\n    listen(server_fd, 3);\n    std::cout << \"Serving on port 8080...\" << std::endl;\n    while (true) {\n        int client_fd = accept(server_fd, nullptr, nullptr);\n        std::string response = \"HTTP/1.1 200 OK\\r\\nContent-Length: 25\\r\\nContent-Type: text/plain\\r\\n\\r\\nHello from C++ Web Server!\";\n        write(client_fd, response.c_str(), response.length());\n        close(client_fd);\n    }\n    return 0;\n}\n"],
    ["cpp_std", "code_dict_data", "#include <map>\n#include <string>\n#include <fstream>\n#include <sstream>\n    std::map<std::string, std::string> dict;\n    std::ifstream file(\"dictionary.tsv\");\n    std::string line;\n    while (std::getline(file, line)) {\n        size_t tab = line.find('\\t');\n        if (tab != std::string::npos) {\n            dict[line.substr(0, tab)] = line.substr(tab + 1);\n        }\n    }\n"]
  ]
}
